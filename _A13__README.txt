Built using powershell(All files are already there).
	Navigate to top directory (You can see the 'gradlew' file/etc)
	run 'gradle build'

See the results
	Navigate to top directory (you can see the 'gradlew' file)
	Goto build>reports>tests>test>Index.html  (will show the results of my tests)

Screenshots
	Saved directory called "Screenshots" in the top directory (You can see the 'gradlew' file)